#!/bin/bash

# Check if exactly one argument is passed
if [ "$#" -ne 1 ]; then
    echo "Usage: $0 <username>"
    exit 1
fi

# Get the username from the first argument
username="$1"

# Use the su command to switch to the specified user and execute the whoami command
su -c "whoami" "$username"
